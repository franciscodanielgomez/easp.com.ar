---
// Highlights Section
// ------------
// Description: A section with a multiple text and image rows.

// Components
// - UI
import TextImage from '../basic/TextImage.astro'

// Content
import highlight01 from '../../../assets/highlights/highlight-01.svg'
import highlight01mobile from '../../../assets/highlights/highlight-01-mobile.svg'
import highlight02 from '../../../assets/highlights/highlight-02.svg'
import highlight02mobile from '../../../assets/highlights/highlight-02-mobile.svg'
import highlight03 from '../../../assets/highlights/highlight-03.svg'
import highlight03mobile from '../../../assets/highlights/highlight-03-mobile.svg'
import highlight04 from '../../../assets/highlights/highlight-04.svg'
import highlight04mobile from '../../../assets/highlights/highlight-04-mobile.svg'

type HighlightBlock = {
    title: string
    text: string
    image: any
    mobileImage?: any
    imageWidth: number
    imageHeight: number
    imagePosition: 'left' | 'right'
}
const highlightBlocks: HighlightBlock[] = [
    {
        title: '<strong>Misión</strong>',
        text: 'Generar espacios de acompañamiento, crecimiento, formación y capacitación, tanto para las familias, profesionales de salud, educación, entidades públicas y privadas. Respetando los procesos fisiológicos, derechos, decisiones, creencias, realidades de vida y cultura durante la preconcepción, gestación, parto, nacimiento, puerperio y primera infancia.',
        image: highlight01,
        imageWidth: highlight01.width,
        imageHeight: highlight01.height,
        mobileImage: highlight01mobile,
        imagePosition: 'right'
    },
    {
        title: '<strong>Visión</strong>',
        text: 'Convertirnos en la entidad referente en salud perinatal de toda la Agentina promoviendo la concientización sobre el valor de este tiempo vital de las familias para así favorecer a las futuras generaciones.',
        image: highlight02,
        mobileImage: highlight02mobile,
        imageWidth: highlight02.width,
        imageHeight: highlight02.height,
        imagePosition: 'left'
    },
    {
        title: '<strong>Valores</strong>',
        text: `En la EASPAC valoramos el acompañamiento cercano, el respeto por la diversidad, y la calidez en el trato. Actuamos con honestidad, promovemos la salud integral de familias, fomentamos la comunicación abierta y defendemos el cuidado de la vida, protegiendo la primera infancia y la maternidad.`,
        image: highlight03,
        mobileImage: highlight03mobile,
        imageWidth: highlight03.width,
        imageHeight: highlight03.height,
        imagePosition: 'right'
    }
]
---

{
    highlightBlocks.map((highlight, key) => (
    <TextImage
            id={`highlight-${key}`}
            title={highlight.title}
            text={highlight.text}
            image={highlight.image}
            imageWidth={highlight.imageWidth}
            imageHeight={highlight.imageHeight}
            mobileImage={highlight.mobileImage}
            imagePosition={highlight.imagePosition}
            classes={key === 0 ? '!pb-0' : key === highlightBlocks.length - 1 ? '!pt-0' : '!py-0'}
    />
        ))
    }
